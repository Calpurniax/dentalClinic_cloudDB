# Firestore db connection
A script with firebase db connection for customer management

## Getting started
This is a simple script in Python to manage the clients information in a Firebase DB.

For testing you need a Firebase project and create a file named "credentials.json".

Once you have the credentials to run the program just type in the terminal

`py main.py`
